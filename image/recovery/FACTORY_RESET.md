# Factory reset and support bundle scope

Recovery features should eventually support:

- reset onboarding state,
- reset runtime config to validated defaults,
- preserve or rotate device identity according to product policy,
- export support bundle with logs, board-pack id, image version, and tuning preset,
- provide guided recovery entry from degraded mode.
